import 'package:flutter/material.dart';

class MiniOnboarding4 extends StatelessWidget {
  const MiniOnboarding4({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/new user homepage onboarding4.png',
      fit: BoxFit.fill,
    );
  }
}
